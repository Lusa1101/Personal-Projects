<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyMoneyPlanner.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25"
            BackgroundColor="#BED2D8">
            <Label Text="" />
            <Image
                Source="logo_generated_power_assistant_bot.jpg"
                HeightRequest="500"
                Aspect="AspectFit"
                SemanticProperties.Description="Omphulusa showing his portrait" />

            <Label
                Text="My Money Planner"
                FontAttributes="Bold"
                FontSize="Header"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Button
                x:Name="Manage"
                Text="Start Managing" 
                SemanticProperties.Hint="Take the user to Money Manager Page"
                Clicked="OnManage"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
